return function(colors)
	return {
		TSAnnotation = {
			fg = colors.base0F,
		},

		TSAttribute = {
			fg = colors.base0A,
		},

		TSTagAttribute = {
			link = "TSProperty",
		},

		TSCharacter = {
			fg = colors.base08,
		},

		TSConstructor = {
			fg = colors.base0C,
		},

		TSConstBuiltin = {
			fg = colors.base09,
		},

		TSConstMacro = {
			fg = colors.base08,
		},

		TSError = {
			fg = colors.base08,
		},

		TSException = {
			fg = colors.base08,
		},

		TSFloat = {
			fg = colors.base09,
		},

		TSKeyword = {
			fg = colors.base0E,
		},

		TSKeywordFunction = {
			fg = colors.base0E,
		},

		TSKeywordReturn = {
			fg = colors.base0E,
		},

		TSFunction = {
			fg = colors.base0D,
		},

		TSFuncBuiltin = {
			fg = colors.base0D,
		},

		TSFuncMacro = {
			fg = colors.base08,
		},

		TSKeywordOperator = {
			fg = colors.base0E,
		},

		TSMethod = {
			fg = colors.base0D,
		},

		TSNamespace = {
			fg = colors.base08,
		},

		TSNone = {
			fg = colors.base05,
		},

		TSParameter = {
			fg = colors.base08,
		},

		TSParameterReference = {
			fg = colors.base05,
		},

		TSPunctBracket = {
			fg = colors.base0F,
		},

		TSPunctDelimiter = {
			fg = colors.base0F,
		},

		TSPunctSpecial = {
			fg = colors.base08,
		},

		TSStringRegex = {
			fg = colors.base0C,
		},

		TSStringEscape = {
			fg = colors.base0C,
		},

		TSSymbol = {
			fg = colors.base0B,
		},

		TSTagDelimiter = {
			fg = colors.base0F,
		},

		TSText = {
			fg = colors.base05,
		},

		TSStrong = {
			bold = true,
		},

		TSEmphasis = {
			fg = colors.base09,
		},

		TSStrike = {
			fg = colors.base00,
			strikethrough = true,
		},

		TSLiteral = {
			fg = colors.base09,
		},

		TSURI = {
			fg = colors.base09,

			underline = true,
		},

		TSTypeBuiltin = {
			fg = colors.base0A,
		},

		TSVariableBuiltin = {
			fg = colors.base09,
		},

		TSVariable = {
			fg = colors.base05,
		},

		TSDefinition = {
			sp = colors.base04,
			underline = true,
		},

		TSDefinitionUsage = {
			sp = colors.base04,
			underline = true,
		},

		TSCurrentScope = {
			bold = true,
		},

		luaTSField = {
			fg = colors.base0D,
		},

		TSFieldKey = {
			fg = colors.base08,
		},

		TSProperty = {
			fg = colors.base08,
		},

		TSInclude = {
			link = "Include",
		},

		TSConditional = {
			link = "Conditional",
		},
	}
end
